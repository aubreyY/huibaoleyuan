var app = getApp();
import $api from '../../base/api'
Page({
    data: {

    },
    //事件处理函数
    my_map: function () {
        wx.navigateTo({
            url: '../map/map'
        })
    },

    onLoad(query) {


    },

    init(nextIndex, menu) {

    },

});